---
description: 功能改动后同步更新 README 与 PROJECT_REFERENCE
alwaysApply: true
---

# 同步项目文档

凡改动会影响「别人 / 下一个会话如何理解本项目」的内容，**同一轮回复内**更新文档，不要等用户再催。

## 必须同步的文件

| 文件 | 更新什么 |
|------|----------|
| `docs/PROJECT_REFERENCE.md` | 布局、组件名、数据流、默认值、色彩规则、导出、决策表、坑、backlog、「改 X 看哪里」；顶部「最后同步」日期 |
| `docs/DESIGN_SPEC.md` | 视觉 token、圆角 smoothing、字体阶梯、组件外观规则（改 chrome 样式时） |
| `README.md` | 简介、功能列表、界面结构概览、运行方式（仅当对外说明变化时） |

## 触发条件（任一即更新）

- 增删 / 重命名 UI 组件或改 App 布局
- 改 `useStudio` 默认值或 store API
- 改 `BLOB_PROFILES`、取色、导出格式、对比度展示
- 改字体策略、快捷键、重要交互
- 关闭或新增已知限制 / backlog 项

## 不做

- 纯样式微调且文档未点名描述的，可不改文档
- 不要新建平行「上下文」文件；单一事实源保持 `docs/PROJECT_REFERENCE.md`
